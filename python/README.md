# Python Query Engine

This directory contains the Python modules responsible for:

- Processing natural-language queries
- Converting user requests into JSON
- Communicating with the C++ backend
- Displaying the algorithm results
